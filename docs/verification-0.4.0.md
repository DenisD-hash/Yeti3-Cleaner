# Проверка 0.4.0 — 24 сентября 2026

- `cargo test`: 24 теста, все прошли.
- Native scanner: точные суммы размеров, сортировка, рекурсивный обход, символическая ссылка с циклом, отмена, отсутствующий корень — прошли.
- Native updater: разрешённый источник, числовое сравнение версий, минимальная версия macOS, отказ при изменённом содержимом — прошли.
- `tests/cleanup_integration.py`: фактическая очистка только во временном HOME; сохранение исключений и выключенных категорий, защищённых документов и вложенного .git, пользовательские папки, дедупликация, отсутствие истории dry-run и остановка при повреждённых правилах — прошли.
- Release build с `-D warnings`: успешно.
- `.app`: codesign --verify --deep --strict — успешно; подпись ad-hoc.
- Основной и графический бинарники: arm64, LC_BUILD_VERSION minos 14.0.
- DMG: hdiutil verify — успешно. Образ смонтирован только для чтения, подпись вложенного приложения проверена, engine --version вернул 0.4.0; образ отключён.
- GUI-процесс карты запущен на каталоге ресурсов проекта. Визуальная проверка и нажатия не выполнены: Computer Use вернул отсутствие разрешений. Это не считается успешным UI smoke-test.
- Не выполнялась очистка настоящего HOME пользователя. Не проверены установка поверх старой версии через Finder, работа на macOS 14/15, Intel, Developer ID и нотариализация.

Проверка обновления и установка различаются: скачивание/контрольная сумма реализованы, установка требует обычной замены приложения через Finder.
